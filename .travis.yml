sudo: required

language: node_js

node_js:
  - "node"

services:
  - docker
  - mysql
  
before_script:
  - sudo /etc/init.d/mysql stop
  - docker-compose pull
  - docker-compose build
  - docker-compose up -d
  - sleep 5
  
script:
  - docker exec altraninnovationraceportal_web_1 npm test
  
after_script:
  - docker-compose kill
