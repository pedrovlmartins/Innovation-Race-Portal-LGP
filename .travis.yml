sudo: required

language: node_js

node_js:
  - "node"

services:
  - docker
  - mysql
  
before_script:
  - sudo /etc/init.d/mysql stop
  - docker-compose up -d
  - sleep 10
  
script:
  - npm test
  
after_script:
  - docker-compose kill
